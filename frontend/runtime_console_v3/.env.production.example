# Produção EC2 + domínio judgetcg.com.br (não commitar .env.local com IP:8000)
API_PROXY_TARGET=http://127.0.0.1:8000
NEXT_PUBLIC_APP_URL=https://judgetcg.com.br
# Deixe NEXT_PUBLIC_API_URL vazio no browser — usa /api/proxy (mesmo origin)
