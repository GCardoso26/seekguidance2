# Produção Vercel + API Render (não commitar secrets)
API_PROXY_TARGET=https://tcg-judge-api.onrender.com
NEXT_PUBLIC_APP_URL=https://judgetcg.com.br
# Deixe NEXT_PUBLIC_API_URL vazio — browser usa /api/proxy (mesmo origin)
