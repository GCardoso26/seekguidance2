import type { TcgType } from "@/types/judge";

/** Alinhado com `PRICING_PLANS` free tier. */
export const FREE_DAILY_QUESTIONS = 50;

export const FREE_TCG_IDS: TcgType[] = ["magic", "pokemon", "yugioh", "lorcana", "one_piece"];
