"use client";

/**
 * @deprecated Prefer `MinimalProviders` + area shells (Marketplace/Seller/Judge).
 * Mantido para referência / imports legacy.
 */
export { MinimalProviders as AppProviders } from "@/providers/MinimalProviders";
