# Kubernetes (MVP)

- `api-deployment.yaml` — Deployment + Service para a API (ajustar imagem, secrets e HPA em produção).
- Próximos passos: HNSW rebuild job, worker Deployment separado, ServiceMonitor Prometheus, OTEL Collector sidecar.
