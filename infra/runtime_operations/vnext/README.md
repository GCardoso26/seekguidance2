# Runtime operations VNext

Extensões incrementais:

- DLQ persistente e políticas de retry (ver `persistent_dlq_runtime/`).
- Armazenamento distribuído de replay e retention (ver `replay_archive_storage/`).
- Semantic snapshots e recovery playbooks (ver `disaster_recovery_v2/` e `runtime_governance/`).

Estes READMEs ligam-se aos stubs em `app/runtime/production_runtime/` e `app/runtime/replay_governance_v2/`.
