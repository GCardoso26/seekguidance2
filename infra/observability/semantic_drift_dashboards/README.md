# Semantic drift dashboards

Suggested panels:

- Ontology volatility (`ontology_drift_tracking_v2`)
- Cross-TCG spread (`cross_tcg_regression_tracking`)

**Gap:** Grafana JSON to be duplicated per environment datasource.
