# WAF (readiness)

- Regras OWASP baseline, rate-based para `/v1/chat` se exposto via API Gateway.
- Integração com `WAF_WEB_ACL_ARN` (config API opcional para validação de presença em smoke internos).
