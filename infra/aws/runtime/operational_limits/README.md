# Operational limits

- Rate limit API (`API_RATE_LIMIT_*`).
- Ingestão: `INGESTION_DEFAULT_HOST_RPS` + `INGESTION_MAX_CONCURRENT_DOWNLOADS`.
- Solver: `FORMAL_SOLVER_TIMEOUT_MS`, `SOLVER_RUNTIME_GPU_ENABLED`.
