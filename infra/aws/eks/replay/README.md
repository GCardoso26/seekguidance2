# Replay (namespace)

Jobs ou deployments dedicados a:

- compactação de filas de replay,
- verificação de integridade (hash lineage),
- exportação para S3 Glacier (lifecycle no bucket).

Integra com `app/runtime/replay_governance_v2/` (adapters AWS opcionais).
