# Grafana dashboards

Importar JSON para Amazon Managed Grafana ou Grafana self-hosted:

- `runtime_governance.stub.json`
- `replay_health.stub.json`

Expandir com queries Prometheus/CloudWatch reais (replay lineage, solver health, workers).
