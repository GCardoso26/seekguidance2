# Mobile runtime readiness

## API

- `app/mobile_runtime/mobile_runtime_readiness.py` agrega sinais de sync (`replay_sync_health_v3_stub`) e contrato `MobileReadinessSignals`.

## Uso

- Checklist pré-release: drift limitado, transporte compacto válido, scoring de conflitos explicável (não punitivo).
