# TOURNAMENT_DOMAIN.md

Event contains **0..N** tournaments via `store_event_tournaments` + nullable `tournaments.store_event_id` / `store_id`.

Operational engine (rounds, pairings, brackets) remains RC1 `app/tournament`.
