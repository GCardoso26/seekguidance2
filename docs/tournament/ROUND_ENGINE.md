# ROUND_ENGINE.md

Round view projects RC1 `tournament_rounds` / current_round.

Timer / extensions: existing RC1 WS/SSE (`/runtime/judge/tournament/timers/*`).  
BP2 RoundService is a read facade only.
