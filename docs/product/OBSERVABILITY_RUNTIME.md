# OBSERVABILITY_RUNTIME.md

**Version:** 1.0.0  
**Module:** `analytics_runtime.observability`

In-process counters/histograms (OTel-compatible naming). Does **not** alter existing tracing middleware.

Prefixes: `runtime.*` · `metric.*` · `aggregation.*` · `scheduler.*` · `cache.*` · `health.*`
