# Saúde operacional do runtime

## Fontes

- `replay_runtime_health`, `continuous_v10`, métricas `runtime_health_metrics`, dashboards `replay_health.json` / `runtime_governance.json` / `runtime_ci.json`.

## Leitura

- Agregar tendências com `rolling_summary` e `replay_health_bundle` nos stubs v10; validar com gates de `runtime_ci`.
