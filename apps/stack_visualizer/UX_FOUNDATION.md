# Stack visualizer — UX foundation

Planned overlays (stubs):

- Stack / chain semantics per TCG adapter (no universal MTG-only model).
- APNAP / priority timeline: data from `app.reasoning.multiplayer` validators.
- Ontology drift badge: optional, feature-flagged.

**Gap:** `index.html` unchanged; hooks documented here for judge console integration.
