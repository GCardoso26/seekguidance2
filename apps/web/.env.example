# Local full vertical (auth + marketplace + cart): AUTH_API_PORT=8789 npm run api:checkout
PUBLIC_API_ORIGIN=http://127.0.0.1:8787
API_ORIGIN=http://127.0.0.1:8789
# Optional override if checkout runs on another port (must share JWT/users with auth)
CHECKOUT_API_ORIGIN=http://127.0.0.1:8789

NEXT_PUBLIC_API_BASE_URL=
