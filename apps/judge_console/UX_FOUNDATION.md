# Judge console — UX foundation

Incremental panels (architecture only):

- Operational confidence: `app.verification.operational_confidence` scores (read-only).
- Replay graph view: IDs + hashes from replay stability bundle.
- Multiplayer: APNAP visualization fed by `validate_apnap_order` outputs.

**Gap:** No large SPA; console HTML stays thin until backend endpoints stabilize.
