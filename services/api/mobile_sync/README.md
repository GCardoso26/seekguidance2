# `services/api/mobile_sync` (legado de path)

A implementação Python vive em `app/mobile_runtime/mobile_sync/` e nas rotas `app/api/v1/mobile.py` (`/v1/mobile/sync/...`).

Este ficheiro existe para alinhar com a estrutura pedida na sprint **sem** duplicar pacotes fora de `app/`.
