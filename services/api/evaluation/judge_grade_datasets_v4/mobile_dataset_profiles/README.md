# Perfis de dataset móvel (`judge_grade_datasets_v4`)

Perfis compactos para **iOS/Android**: subconjuntos judge-grade com **lineage** e **manifestos** versionados. Integração incremental com `v4_dataset_stub` — sem substituir manifests completos na cloud.
