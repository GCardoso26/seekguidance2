# Judge-grade datasets v3

Manifestos executáveis com **replay snapshots**, metadados de **ontology drift** e **branch divergence**, mantendo soft normalization e payloads explainability-first.

Ver `schemas/dataset_manifest_v3.schema.json` e `manifests/stub_v3.json`.
