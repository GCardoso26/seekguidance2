# Golden samples (human-reviewed)

Colocar aqui JSON/YAML com:

- `question`, `game_slug`, `gold_answer`, `gold_citations`, `gold_reasoning_trace` (opcional).

Integrar com pipelines de calibração e `evaluation_worker` (arq).
