# Golden human answers

Conjunto de respostas revistas por juízes / peritos. Cada entrada referencia `case_id`, `game_slug`, `expected` (cadeias de legalidade, timing, replay) e metadados de revisão.

Ver `schema.json`. Os ficheiros JSONL devem ser tratados como dados sensíveis (não versionar PII).
