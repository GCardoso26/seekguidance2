"""FAB — reações / recursão de combat chain."""

from __future__ import annotations


def fab_reaction_recursion_stub(depth: int) -> dict[str, object]:
    return {"recursion_pressure": depth > 6, "depth": depth}
