"""MTG multiplayer — loops APNAP / replacement."""

from __future__ import annotations


def mtg_apnap_loop_stub(rounds: int) -> dict[str, object]:
    return {"loop_pressure": rounds > 8, "rounds": rounds}
