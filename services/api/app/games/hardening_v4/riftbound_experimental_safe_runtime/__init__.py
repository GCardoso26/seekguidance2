"""Riftbound — experimental com segurança de abstração."""

from __future__ import annotations

from typing import Any


def riftbound_experimental_safe_runtime_stub(unknown_tags: list[str]) -> dict[str, Any]:
    return {
        "unknown_tags": unknown_tags,
        "assistant_notes": ["Unknown abstraction safety; sem equivalência com outros TCGs."],
    }
