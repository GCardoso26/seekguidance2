"""Divergência semântica ao longo do tempo."""

from __future__ import annotations

from typing import Any


def semantic_divergence_tracking_stub(samples: list[float]) -> dict[str, Any]:
    return {"max": max(samples) if samples else 0.0}
