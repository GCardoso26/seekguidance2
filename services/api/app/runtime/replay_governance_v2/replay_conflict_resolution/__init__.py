"""Resolução de conflitos de replay."""

from __future__ import annotations

from typing import Any


def replay_conflict_resolution_stub(conflicts: int) -> dict[str, Any]:
    return {"conflicts": conflicts, "assistant_notes": ["Conflitos assistentes; juiz decide."]}
