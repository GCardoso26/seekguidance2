"""Merge temporal de eventos de replay (stub)."""

from __future__ import annotations

from typing import Any


def replay_temporal_merge_bridge_stub(replay_ref: str) -> dict[str, Any]:
    return {
        "replay_ref": replay_ref,
        "temporal_merge_summary": {"ordering": "apnap_like_hints_stub"},
        "assistant_notes": ["replay_temporal_merge_bridge: timing por TCG via soft normalization."],
    }
