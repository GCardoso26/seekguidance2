"""Caps de ramos distribuídos."""

from __future__ import annotations

from typing import Any


def distributed_branch_caps_stub(shards: int, cap_each: int) -> dict[str, Any]:
    return {"shards": shards, "cap_each": cap_each, "assistant_notes": ["Multiplayer branch explosion control."]}
