"""Conflitos temporais em replay (diagnostics v2)."""

from __future__ import annotations

from typing import Any


def replay_temporal_conflicts_v2_stub(replay_ref: str) -> dict[str, Any]:
    return {
        "replay_ref": replay_ref,
        "temporal_conflicts": [],
        "assistant_notes": ["replay_temporal_conflicts: APNAP/SEGOC como hints por TCG."],
    }
