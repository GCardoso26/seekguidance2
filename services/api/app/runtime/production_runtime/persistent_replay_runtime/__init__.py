"""Replay persistente em runtime."""

from __future__ import annotations

from typing import Any


def persistent_replay_runtime_stub(retention_days: int) -> dict[str, Any]:
    return {"retention_days": retention_days, "assistant_notes": ["Replay storage governance."]}
