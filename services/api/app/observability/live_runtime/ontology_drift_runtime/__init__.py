"""Drift de ontologia em runtime."""

from __future__ import annotations

from typing import Any


def ontology_drift_runtime_stub(game: str, delta: float) -> dict[str, Any]:
    return {
        "game": game,
        "delta": delta,
        "assistant_notes": ["Soft normalization: drift mede alinhamento assistente, não verdade absoluta."],
    }
