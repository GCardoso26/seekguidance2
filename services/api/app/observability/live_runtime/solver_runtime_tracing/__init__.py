"""Tracing do solver em runtime."""

from __future__ import annotations

from typing import Any


def solver_runtime_tracing_stub(session_id: str) -> dict[str, Any]:
    return {"session_id": session_id, "assistant_notes": ["Proof steps agregados, nunca CNF bruto."]}
