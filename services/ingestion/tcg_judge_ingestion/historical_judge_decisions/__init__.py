"""Decisões históricas de juiz (metadados)."""

from __future__ import annotations


def judge_decision_record_stub(case_id: str) -> dict[str, str]:
    return {"case_id": case_id, "tier": "judge_assistant"}
