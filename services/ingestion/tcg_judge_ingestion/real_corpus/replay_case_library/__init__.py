"""Biblioteca de casos de replay (IDs + tags)."""

from __future__ import annotations


def replay_case_stub() -> dict[str, object]:
    return {"cases": ["replay_determinism_smoke", "branch_prune_smoke"], "note": "Expandir com dataset real."}
