# context/post-beta-backlog.md

Itens descobertos na resolução de blockers RC1 — **não implementar agora**.

- [ ] Expor schema `tcg_judge` no PostgREST (Supabase API Settings) para probe direto `card_catalog` no BFF
- [ ] `/loja` atualmente faz bailout para CSR — melhorar SSR/SEO (impacta Lighthouse)
- [ ] GitHub Actions: restaurar billing/spending limit (bloqueio externo)
- [ ] Alinhar `SHIPPING_V2_ENABLED=true` no backend Render staging/prod após Go
- [ ] Remover tema `#7c3aed` residual em meta theme-color se DS v3 pedir outra accent
- [ ] ESLint warnings (~60) — triagem pós-beta
- [ ] axe-core CI formal nos fluxos buyer
- [ ] Inventory avançado (XLSX, drag columns, ABC, rollback import)
